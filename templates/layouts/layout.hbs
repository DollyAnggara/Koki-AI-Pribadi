<!doctype html>
<html lang="id">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <title>{{#if title}}{{title}}{{else}}Koki AI Pribadi{{/if}}</title>
    <link rel="stylesheet" href="/css/style.css">
  </head>
  <body class="{{#if isAdmin}}is-admin{{/if}}">
    {{> header}}

    <!-- Toast notifications (top-right) -->
    <div class="kontainer-toasts" id="kontainerToasts"></div>
    <!-- Modal notifications (centered with backdrop) -->
    <div class="kontainer-modal" id="kontainerModalNotifikasi" aria-hidden="true"></div>

    {{{body}}}

    <!-- Socket.io + frontend script -->
    <script src="https://cdn.socket.io/4.7.2/socket.io.min.js"></script>
    <script src="/js/app.js"></script>
  </body>
</html>