<div class="admin-page">
  <div class="admin-container">
    <!-- Header -->
    <div class="admin-header">
      <div class="admin-header-content">
        <div class="admin-header-info">
          <h1>Kelola Resep</h1>
          <p>Lihat, edit, dan kelola semua resep di situs Anda</p>
        </div>
        <div class="admin-header-actions">
          <a href="/admin/resep/new" class="admin-btn admin-btn-primary">
            <span>➕</span>
            Tambah Resep
          </a>
          <a href="/admin/resep/pending" class="admin-btn admin-btn-warning">
            <span>⏳</span>
            Pending ({{stats.pendingCount}})
          </a>
          <a href="/admin/dashboard" class="admin-btn admin-btn-secondary">
            <span>←</span>
            Kembali
          </a>
        </div>
      </div>
    </div>

    <!-- Search & Filter -->
    <div class="admin-card">
      <div class="admin-search-bar">
        <input
          id="resepSearch"
          type="search"
          placeholder="Cari resep, kategori, pengirim..."
          class="admin-search-input"
          aria-label="Cari resep"
        />
        <span style="color: var(--admin-text-light); font-weight: 600;">
          Total:
          <strong>{{resep.length}}</strong>
        </span>
      </div>
    </div>

    <!-- Recipes Table -->
    <div class="admin-card">
      <div class="admin-table-wrapper">
        <table class="admin-table">
          <thead>
            <tr>
              <th>Nama Resep</th>
              <th>Kategori</th>
              <th>Status</th>
              <th>Pengirim</th>
              <th style="text-align: right;">Aksi</th>
            </tr>
          </thead>
          <tbody>
            {{#if resep.length}}
              {{#each resep}}
                <tr>
                  <td class="table-column-name">{{this.namaResep}}</td>
                  <td>
                    <span
                      class="admin-badge admin-badge-info"
                    >{{this.kategoriLabel}}</span>
                  </td>
                  <td>
                    {{#if this.isApproved}}
                      <span class="admin-badge admin-badge-success">✓ Disetujui</span>
                    {{else if this.isPending}}
                      <span class="admin-badge admin-badge-warning">⏳ Pending</span>
                    {{else if this.isRejected}}
                      <span class="admin-badge admin-badge-danger">✗ Ditolak</span>
                    {{else}}
                      <span
                        class="admin-badge admin-badge-primary"
                      >{{this.status}}</span>
                    {{/if}}
                    {{#if this.approvedAt}}
                      <div
                        style="font-size: 0.85rem; color: var(--admin-text-light); margin-top: 4px;"
                      >
                        {{this.approvedAt}}
                      </div>
                    {{/if}}
                  </td>
                  <td>
                    {{#if this.submittedBy}}
                      {{#if this.submittedBy.namaPengguna}}
                        {{this.submittedBy.namaPengguna}}
                      {{else}}
                        {{this.submittedBy}}
                      {{/if}}
                    {{else}}
                      <span style="color: var(--admin-text-light);">-</span>
                    {{/if}}
                  </td>
                  <td style="text-align: right; white-space: nowrap;">
                    <a
                      href="/admin/resep/{{this._id}}/edit"
                      class="admin-btn admin-btn-secondary admin-btn-small"
                    >
                      ✏️ Edit
                    </a>
                    <form
                      method="post"
                      action="/admin/resep/{{this._id}}/delete"
                      style="display:inline"
                      onsubmit="return confirm('Hapus resep ini?');"
                    >
                      <button
                        class="admin-btn admin-btn-danger admin-btn-small"
                      >
                        🗑️ Hapus
                      </button>
                    </form>
                  </td>
                </tr>
              {{/each}}
            {{else}}
              <tr>
                <td
                  colspan="5"
                  style="text-align: center; padding: 40px 20px; color: var(--admin-text-light);"
                >
                  <p style="margin: 0; font-size: 1rem;">📭 Tidak ada resep</p>
                  <p style="margin: 8px 0 0 0; font-size: 0.9rem;">
                    <a
                      href="/admin/resep/new"
                      style="color: var(--admin-accent); text-decoration: none; font-weight: 600;"
                    >Buat resep pertama Anda</a>
                  </p>
                </td>
              </tr>
            {{/if}}
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>

<script src="/js/admin.js"></script>